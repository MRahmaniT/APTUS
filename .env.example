# Frontend data source.
# Use "server" for the self-hosted PostgreSQL API. "local" keeps the browser-only demo fallback.
VITE_DATA_MODE=server

# Same-origin API in production. During Vite development /api and /uploads are proxied to localhost:3000.
VITE_API_BASE=/api
